spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/spotyourslot}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
